# EXP-001 — FLUX.2 Klein free-GPU feasibility

**Progress: BLOCKED. Classification: RED for the currently available execution environment. Model feasibility: UNKNOWN.** Prepared and updated 2026-09-21. An initial CPU-only allocation could not launch `nvidia-smi`; a later Supervisor-run audit reported T4 ×2. The full harness and its one kernel-bound discriminating retry both used CPU-only Torch and could not expose CUDA, so EXP-001 stopped before downloads under its retry rule.

## Question and hypothesis

Can `black-forest-labs/FLUX.2-klein-base-4B` load, perform image editing, complete a tiny paired edit-LoRA training cycle, save an adapter, and reload it for inference on the Supervisor's free Kaggle GPU? Can the Base-trained adapter execute with `black-forest-labs/FLUX.2-klein-4B` distilled inference?

The hypothesis is feasibility under documented low-memory settings. [BFL training guidance](https://docs.bfl.ai/flux_2/flux2_klein_training) lists an NVIDIA GPU with 12 GB VRAM and 32 GB RAM as its Klein 4B Base training minimum, while [BFL's worked LoRA guide](https://huggingface.co/blog/black-forest-labs/flux-2-klein-lora) describes a run under 24 GB VRAM on an RTX 4090. Neither establishes performance on this Kaggle account. The [Base model card](https://huggingface.co/black-forest-labs/FLUX.2-klein-base-4B) verifies the model ID, image-edit capability, and Apache-2.0 license. [NVIDIA's CUDA guide](https://docs.nvidia.com/cuda/cuda-programming-guide/05-appendices/mathematical-functions.html) places native BF16 at compute capability 8.0+, so a runtime with capability 7.5 must use an evidence-backed non-BF16 path.

## Reproducible harness

Source: [Kaggle smoke-test script](../../notebooks/exp001_flux2_klein_kaggle_smoke.py). It is a single CLI artifact because dependency installation and model execution need separate Python processes; it contains ordered audit, preflight, Base load, Base edit, tiny training, Base adapter reload, and optional distilled compatibility stages. It does not bundle model weights or real photos.

In an authenticated Kaggle GPU notebook, enable internet and GPU, make the script available as a notebook input or in `/kaggle/working`, and run:

```python
import subprocess, sys

probe = subprocess.run(
    [sys.executable, "-c", "import torch; assert torch.cuda.is_available()"],
    check=False,
)
assert probe.returncode == 0, "The kernel-selected child process has no CUDA"
subprocess.run([sys.executable, "/kaggle/working/exp001_flux2_klein_kaggle_smoke.py"], check=False)
```

The assertion proves that a fresh child process from the notebook kernel's selected executable sees CUDA. The harness starts with another audit and stops before model download if GPU or scratch checks fail. It writes durable evidence to `/kaggle/working/exp001/`; model weights use `/tmp/hf-cache` and the AI Toolkit checkout uses `/tmp/exp001-ai-toolkit`. Save or download `/kaggle/working/exp001/` before the session ends. The script measures actual disks and mounts before downloading.

The script looks for an optional `HF_TOKEN` in Kaggle Secrets or environment variables only if supplied; it never prints or stores the token. The public model is queried for access and file sizes before weights download. It pins [AI Toolkit](https://github.com/ostris/ai-toolkit) to commit `a8dfcf7d7e2b38ccc7b2fb68ece9c6358e61e7a7`, records installed packages, constrains pip to the observed Kaggle Torch/Torchvision/Torchaudio builds, and stops if Torch or CUDA changes. This avoids AI Toolkit's current manager path, which selects its own Torch/CUDA stack. The dataset uses the paired `control_path` format documented by [BFL's edit-LoRA recipe](https://huggingface.co/blog/black-forest-labs/flux-2-klein-lora).

## Intended tiny configuration

- Three deterministic 512×512 Pillow shape pairs, with matching `reference/` and `target/` names and target-side instruction captions. No hairstyle data.
- Single GPU `cuda:0`; 20 steps; batch size 1; 512 resolution; AdamW 8-bit; learning rate `1e-4`; LoRA linear rank/alpha 16/16 and conv 8/8. These are smoke-test settings, not selected production hyperparameters.
- FP16 on the observed T4 because its compute capability is 7.5. `torch.cuda.is_bf16_supported()` reporting true is recorded but does not establish native BF16 Tensor Core acceleration. AI Toolkit `quantize`, `low_vram`, and gradient checkpointing are enabled from the documented low-memory direction. The actual runtime must prove compatibility.
- Base edit uses seed 1977, 512×512, 20 inference steps, guidance 4.0, and the prompt in the script. Distilled test, only if scratch permits, uses four steps and guidance 1.0.

## Evidence to preserve

Expected files, only if the corresponding stage executes: `environment.json`, `nvidia-smi.txt`, `mounts.txt`, `preflight.json`, `dataset_manifest.json`, `dependencies.json`, exact `train_config.yaml`, `train.log`, `training_result.json`, output PNG/JSON files, the small `.safetensors` checkpoint, `result.json`, and `error.txt` on failure. The script logs commands, exceptions, load and inference times, observed GPU peaks, and progress-derived step timing if enough progress intervals appear. It reports 500/1000/1500-step projections only from measured step intervals; startup-inclusive wall time is labeled separately. These projections are not a quality or deadline guarantee.

## Verified environment

Supervisor-run audit output:

| Item | Observed value |
| --- | --- |
| GPU 0 | Tesla T4; 14.56 GB usable VRAM; compute capability 7.5 |
| GPU 1 | Tesla T4; 14.56 GB usable VRAM; compute capability 7.5 |
| GPU use plan | GPU 0 only; memory is not pooled |
| System RAM | 31.35 GB |
| Python | 3.12.13 |
| PyTorch | 2.10.0+cu128 |
| Torch CUDA runtime | 12.8 |
| NVIDIA driver / maximum CUDA | 580.159.04 / 13.0 |
| CUDA available | true |
| Torch BF16 check | true; native T4 BF16 Tensor Core acceleration absent |
| Free disk | approximately 1,024 GB on `/` and `/tmp`; 19.5 GB on `/kaggle/working` |

The driver maximum and Torch CUDA runtime need not match. The functioning CUDA stack will remain in place unless a reproducible dependency error identifies it as the cause.

## Current results

| Required evidence | Result |
| --- | --- |
| Actual Kaggle GPU, VRAM, CUDA, BF16, Python, PyTorch, RAM, disk | VERIFIED above; environment gate GREEN. |
| First full-harness process | STOPPED before downloads: `/usr/bin/python3`, Torch `2.10.0+cpu`, CUDA false, zero GPUs, `nvidia-smi` unavailable. |
| Kernel-bound discriminating retry | STOPPED before harness launch: `sys.executable` was `/usr/bin/python3`; Torch `2.10.0+cpu`; `torch.version.cuda` was `None`; CUDA false. |
| Base load and image edit | NOT STARTED |
| Tiny dataset accepted and optimizer steps | NOT STARTED |
| Checkpoint creation, size, and fresh Base reload | NOT STARTED |
| Distilled adapter compatibility | NOT STARTED |
| Measured seconds/step and projections | UNKNOWN — no measurements |
| GREEN / YELLOW / RED feasibility classification | RED for the currently available Kaggle execution environment; FLUX-specific feasibility remains UNKNOWN. |

No model download, model execution, dependency installation, or training occurred. Therefore dependency versions beyond the preinstalled CPU environment, model-load time, edit result, optimizer steps, loss, seconds per step, VRAM, checkpoint, reload, distilled compatibility, and 500/1000/1500-step projections are all unavailable. A local Python syntax check passed, and the Pillow generator created three matching test pairs in a temporary directory; these checks do not establish Kaggle compatibility.

## Failures, retries, and conclusion

The full-harness attempt launched its prepare phase as `/usr/bin/python3` and observed `torch 2.10.0+cpu`, `torch.cuda.is_available() == false`, `nvidia-smi` exit 127, and no GPU devices. It raised `RuntimeError: STOP: no CUDA GPU was allocated; enable a free GPU session before model downloads` after approximately 3.9 seconds. The evidence-backed hypothesis was a shell interpreter mismatch. The single discriminating change launched a CUDA probe with the active notebook kernel's `sys.executable`; it again resolved to `/usr/bin/python3` with CPU-only Torch, CUDA `None`, and `torch.cuda.is_available() == false`, raising `AssertionError: Child process has no CUDA`. This rules out the shell alias explanation for that notebook process. It does not reconcile the earlier T4 audit, but it is sufficient to stop rather than alter CUDA, PyTorch, precision, or model dependencies without a visible GPU.

**Exactly one next action:** the Supervisor and Project Lead should restore a Kaggle notebook whose active kernel and `sys.executable` child both report CUDA before authorizing another EXP-001 run.
