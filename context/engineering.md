# Engineering facts

As inspected 2026-09-22. Evidence status: VERIFIED for observed filesystem and supplied runtime output; all unbuilt application details UNKNOWN.

Before context setup, `F:\HAIR` contained no files. EXP-001 setup initialized local Git on `main` and committed the accepted context baseline as `78c8c64e20fdfe19c956a62cc044f9b095590a07`, including `.gitignore`. `origin` is the Supervisor-supplied `https://github.com/mark-juswa/CometicsAI.git`; the baseline and prepared harness commits were pushed to `origin/main`.

Observed local command availability during setup: Node.js `v24.7.0`, npm/npx `11.5.1`, Git `2.46.0.windows.1`. These are machine observations. The later System MVP section records the actual local Python runtime, selected dependencies, source tree, startup commands, and tests; real model and deployment settings are still unimplemented.

JSM skills were installed with `npx --yes skills@latest add jsmastery-pro/skills -a codex -y` into `.agents/skills/`; `skills-lock.json` was created. The nine folders are architect, audit, check, debug, develop, document, scope, sync, and test. Each has `SKILL.md`. These generic skills may mention agent tools or review modes that differ from this Codex session; follow available tools and project authority, and do not run a skill merely because it exists.

EXP-001 harness: `notebooks/exp001_flux2_klein_kaggle_smoke.py`. Local verification: Python 3.11.5 compiles its syntax, and the Pillow generator produced three matching synthetic pairs in a temporary directory. The harness pins AI Toolkit commit `a8dfcf7d7e2b38ccc7b2fb68ece9c6358e61e7a7`, constrains dependency resolution to the Kaggle-provided Torch build, uses `/tmp/hf-cache`, and stops if Torch/CUDA changes. AI Toolkit dependencies and all model/training behavior remain NEEDS VERIFICATION until the harness runs in Kaggle.

The Supervisor's later EXP-001 audit reported a working Kaggle allocation with two independent Tesla T4 GPUs, each with 14.56 GB usable VRAM and compute capability 7.5; 31.35 GB system RAM; Python 3.12.13; PyTorch 2.10.0+cu128; Torch CUDA 12.8; NVIDIA driver 580.159.04; and `torch.cuda.is_available() == true`. Torch reports BF16 support, but compute capability 7.5 has no native BF16 Tensor Core path, so the harness selects FP16. Reported free disk was approximately 1,024 GB on `/` and `/tmp`, and 19.5 GB on `/kaggle/working`. These are EXP-001 observations, not guaranteed Kaggle specifications. The earlier CPU session without `nvidia-smi` remains recorded as a failed allocation attempt.

A subsequent harness launch ran `/usr/bin/python3` and observed `torch 2.10.0+cpu`, no CUDA devices, and no `nvidia-smi`; it stopped before dependency installation or model download. This conflicts with the earlier notebook-kernel audit and does not establish that the T4 allocation or FLUX model failed. The next discriminating run uses the active notebook kernel's `sys.executable` and stops again if that interpreter also lacks CUDA.

The kernel-bound discriminating retry used `sys.executable == /usr/bin/python3` and again observed Torch `2.10.0+cpu`, `torch.version.cuda == None`, and `torch.cuda.is_available() == false`. That attempt stopped before launching the harness and produced no model or training measurements. It was later superseded by the successful CUDA-enabled launch recorded below.

A later Supervisor-run Kaggle attempt superseded that temporary CPU-runtime observation: CUDA on T4 worked, FLUX.2 Klein Base loaded, a 512×512 Base edit completed, the paired dataset was detected, and LoRA modules initialized. Training then stopped before optimizer steps because AI Toolkit's generic `ddpm` config path called a dynamic `FlowMatchEulerDiscreteScheduler.set_timesteps` without the required `mu`. The pinned toolkit's FLUX route is selected by `train.noise_scheduler: "flowmatch"`; its custom `set_train_timesteps` derives resolution-dependent shifting from the batch latents.

The corrected flow-matching retry reached 20/20 loop steps with a measured median 2.007 seconds per step and 10,779 MiB whole-GPU peak. The progress display printed `loss: 0.000e+00`, while AI Toolkit emitted `loss is nan` once per step; the loss was therefore non-finite and the training result is invalid. AI Toolkit still saved `exp001_tiny_edit.safetensors` at 46,223,600 bytes. A fresh Base pipeline loaded in 80.545 seconds, loaded that adapter, and completed one 512×512 edit inference in 64.962 seconds with peak allocated/reserved memory of 8,705,465,856/8,965,324,800 bytes. Serialization and loader compatibility are VERIFIED; meaningful parameter learning is not.

The authorized BF16 probe was launched later but stopped before loading the trainer or running a step because `/kaggle/working/exp001/environment.json` was absent. Its recorded `bf16_executed` value is false. This run provides no BF16 loss, timing, VRAM, checkpoint, or reload evidence.

After rebuilding the missing session prerequisites, the unchanged BF16 probe completed three finite-loss steps and a separate 20-step finite-loss confirmation on GPU 0. Training used BF16 while checkpoint serialization and the fresh Base pipeline used FP16. The 20-step timing was 32.808075 seconds per step with 13,531 MiB whole-GPU peak. The 46,223,600-byte adapter loaded into a fresh Base pipeline; this retry did not run adapter inference. See [EXP-001 evidence](../docs/experiments/EXP-001.md). The corresponding 500/1,000/1,500-step projections are 4.557/9.113/13.670 hours, excluding setup and save overhead; they are not measured longer runs.

The System MVP was built and checked locally on Node.js `24.7.0`, npm `11.5.1`, and Python `3.11.5`. Frontend: Next.js `16.3.5`, React `19.2.8`, Tailwind CSS `4.3.3`, TypeScript `5.9.3`; the official initializer created `frontend/` with an npm lockfile. Backend: FastAPI `0.111.0`, Uvicorn `0.30.1`, Pillow `10.1.0`, and python-multipart `0.0.22`, pinned in `backend/requirements.txt`. The backend has only `/health`, `/styles`, and `/generate`; it has no database or GPU dependency. `MockEngine` returns a JPEG copy after EXIF correction and RGB conversion. The local API returned HTTP 200 for health, styles, and a multipart mock generation. Backend tests: 6 passed. Frontend lint and production build passed. Microsoft Edge browser tests: 4 passed, covering the full flow, offline errors, and narrow-screen layout. Exact startup commands are in [README](../README.md). No external deployment or real LoRA inference was attempted.

DATA-001 local inspection: the pinned Hugging Face dataset archive is 222,928,302 bytes and contains 30 decodable original photos each in `image/CrewCut`, `image/BobHair`, and `image/LayeredHair`; counting macOS `__MACOSX` metadata files would incorrectly double that number. `scripts/inspect_source_archive.py` reads the ZIP directory through HTTP byte ranges; `scripts/data001_inspect.py` generated the local candidate sheets; `scripts/data001_selection_audit.py` recorded 30 provisionally selected photographs and 60 excluded candidates in `docs/data/DATA-001-candidates.json`. One exact source duplicate (`CrewCut/4.jpg` and `CrewCut/23.jpg`) was detected, with only the latter selected. The Kaggle generation script and guarded finalizer are implemented but have not run on generated hairstyles. See [DATA-001](../docs/data/DATA-001.md).

The DATA-001 implementation review on 2026-09-23 found the pilot planner defaults to `CrewCut_1`, `BobHair_1`, and `LayeredHair_4`, all in train. The Kaggle runner now writes a deterministic plan, requires a REGENERATE review before its one seed-only retry, and requires explicit pilot acceptance plus Supervisor/Project Lead approval before `--all`. Each generation sidecar records source/model revisions, hashes, sample ID, prompt, seed, attempt, timestamp, runtime, dimensions, and GPU memory. The finalizer checks explicit ACCEPT decisions with notes, generation provenance and hashes, RGB images, 24/6 identity-group split, 48/12 pair distribution, caption alignment, and duplicate hashes before reporting the dataset ready. These are code-level checks only; no DATA-001 FLUX pilot has run. The local machine has no Kaggle API credentials and the available in-app browser has no Kaggle session.

Local DATA-001 checks: Python compilation passed; the code-generated three-job pilot plan matched `docs/data/DATA-001-pilot-plan.json`; missing full-generation approval and missing retry review stopped before CUDA/model access. A 30-identity synthetic fixture confirmed 60 directional pairs with the specified class distribution, and a deliberately corrupted generated-image hash stopped the finalizer before it wrote an output dataset. No real generation quality or human acceptance was inferred from these fixture checks.

The Supervisor's 2026-09-23 DATA-001 Kaggle console log shows a successful default three-image TRAIN pilot using Tesla T4 GPU 0, PyTorch `2.10.0+cu128`, and FP16 Diffusers Base pipeline with CPU offload. It printed `CrewCut_1 → BobHair` in 62.1 seconds, `BobHair_1 → LayeredHair` in 64.0 seconds, and `LayeredHair_4 → CrewCut` in 63.3 seconds; the process exit code was 0. The log reports a review sheet path; the subsequent ZIP supplied the pixels and sidecars for inspection. See [DATA-001 pilot console](../docs/data/DATA-001-pilot-console.log). No official review decision or full-generation approval has been issued.

The Supervisor subsequently supplied the pilot ZIP. Its SHA256 was `ef71eb19b52c2d0cf1ad21c8d5f30a36a118bf9319192dc78485e1ccc04d4485`; all three 512×512 RGB original/generated images matched their sidecar hashes and the committed plan. The Base model revision was `a3b4f4849157f664bdbc776fd7453c2783562f4d`; peak allocated GPU memory was about 8,302–8,304 MiB for each edit. Codex visual review found `CrewCut_1` potentially suitable, `BobHair_1` with major blonde-to-brunette drift, and `LayeredHair_4` with facial/accessory/clothing drift. This is one potentially suitable of three, meeting the approved stop threshold. See [pilot review](../docs/data/DATA-001-pilot-review.md). No official review manifest, retry, or full-generation approval exists.

DATA-001 V2 and TRAIN-001 handoff code was inspected and prepared after the Supervisor's execution split. `notebooks/data001_pilot_v2_masked_kaggle.py` is the three-image masked runner; `notebooks/data001_bulk_v2_kaggle.py` plans 27 remaining identities without download by default and requires V2 ACCEPT reviews plus named approval for `--execute`. Its `--retry` is limited to one non-pilot identity with an explicit REGENERATE review and changes only the seed, preserving attempt 1. `scripts/data001_finalize.py --generation-format v2` checks V2 mask hashes and outside-mask pixel preservation before constructing pairs. `notebooks/train001_kaggle.py` validates final manifests/reviews/QA before writing the BF16 250/500-step configuration, and has explicit training/evaluation phases. These are implementation facts from code inspection and short static checks only. No V2 inference, bulk generation, real hairstyle training, or held-out model evaluation has run.

The Supervisor's later critical-path direction makes V1/global generation the active DATA-001 path and leaves masked V2 optional. The V1 runner's `--all` now checks the three existing pilot files, sidecars, and hashes, then selects the 27 remaining identities without a V2 or pilot-acceptance file. `--pilot-zip` restores only the expected three pilot originals/results/sidecars in a fresh Kaggle session. `--plan-all` is read-only. A single seed-only retry requires an explicit REGENERATE review, and existing outputs are skipped only with matching hashes. The model revision is pinned to the observed V1 pilot SHA. Local short checks verified the supplied V1 ZIP can be restored and validated, the planner reports 27 jobs, and Python compilation passes. The 27-image generation remains unrun.

The Supervisor later ran and completed the V1/global bulk generation in Kaggle. The received `data001_v1_unreviewed.zip` contained 30 selected originals, 30 generated PNGs, and 30 matching generation sidecars. Read-only archive inspection verified approved 8/2 splits per class, matching source/output SHA256 values, RGB 512×512 decoding of all 60 images, and no exact duplicate image hashes. All 30 supplied review entries were PENDING. The practical completion handoff proposes one seed-only retry each for `LayeredHair_4` and `LayeredHair_9`; no retries or final directional pairs have been produced yet.

The current `notebooks/train001_kaggle.py` accepts only the first 250-step run. Its generated configuration preserves EXP-001 BF16/flowmatch/quantized low-VRAM LoRA settings, saves at 125 and 250, and retains `.safetensors` checkpoints plus AI Toolkit's `optimizer.pt` for a possible later continuation. The runner stops on non-finite loss and reports success only after finite final-step progress, trainer exit 0, and checkpoint/optimizer files and save messages. Evaluation remains a separate phase. This is code-level preparation verified by Python compilation and replaying the parser against the saved 20-step BF16 log; no real TRAIN-001 job or same-run 500-step resume has executed.

The Supervisor's next Kaggle session reported `fatal: cannot change to '/kaggle/working/CometicsAI'`, confirming that the prior working checkout was absent. The DATA-001 completion handoff now clones the configured GitHub repository when needed and restores the saved 30-image archive from either an uploaded ZIP or Kaggle's extracted Input directory before any review or retry. This is an operational recovery path; no additional image generation is required to recover the archived outputs.

Later Supervisor evidence supersedes the earlier pending-state statements above: the V1 finalizer reported 48 train/12 validation directional pairs and passing file/split QA. The prepared `notebooks/train001_kaggle.py` workflow then completed its real 250-step BF16 run with process exit 0 and a `success: true` summary; see [TRAIN-001 evidence](../docs/experiments/TRAIN-001.md). The final adapter and optimizer state were saved. Held-out evaluation has not yet run, so no model-quality claim follows from these mechanics.

2026-09-24 implementation update: the Supervisor supplied the TRAIN-001 backup and held-out sheet. The 250-step adapter was packaged into ignored `artifacts/train001_adapter_bundle/` with SHA-256 `7e3991f8a4e502573d3e82e9ac34c89fdf3f0b66fb337abb026a5b4c9ad080ff`; the bundle includes metadata tying it to TRAIN-001, the pinned Base revision, and three style IDs. The repository now contains a Kaggle inference bootstrap, pinned/constrained inference requirements, a FastAPI GPU server, a temporary Cloudflare tunnel readiness check, a local `RemoteFluxEngine`, a single-request test client, and a one-cell notebook. Eight local Python API tests, frontend lint/build, and five Edge browser tests (four mock and one mocked remote flow) passed. A real GPU load, tunnel, and full browser-to-GPU request have not yet run, so live serving is NEEDS VERIFICATION. The held-out evaluation and DATA-001 visual inspection found quality concerns; the current checkpoint is experimental and not visually cleared.

Initial `git push origin main` attempts failed because this machine temporarily could not resolve `github.com`. A later push succeeded, publishing the runtime and fallback commits to `origin/main`. The source-code ZIP under ignored `artifacts/` remains an optional Kaggle Input fallback; the notebook setup cell can restore from either that archive or a GitHub checkout.

The Supervisor's first fresh-session Kaggle runtime audit on 2026-09-24 reached dependency setup with Python 3.12.13, CUDA-enabled PyTorch 2.10.0+cu128, and one Tesla T4 with 14.56 GiB reported VRAM. The pip log showed a direct constraint conflict: repository-pinned Diffusers 0.40.0 requires `huggingface-hub>=1.23.0,<2.0`, while the original inference requirements demanded `huggingface-hub>=0.34,<1`. Inspection of the upstream package metadata also found Diffusers 0.40.0 requires `safetensors>=0.8.0`; the preinstalled Kaggle version was 0.7.0. The inference requirements now specify Transformers 5.0.0 (already installed in that Kaggle runtime), Hub `>=1.23,<2`, and Safetensors `>=0.8,<1`, while retaining the Kaggle Torch build constraint. These corrected requirements and live FLUX import still need a Kaggle rerun; the pip resolution failure establishes no model-serving result.

The Supervisor reran the corrected bootstrap. Pip completed, `Flux2KleinPipeline` imported, the TRAIN-001 adapter bundle passed SHA-256 verification, and the pinned Base snapshot downloaded. The GPU server loaded all five Base pipeline components, then its LoRA injection failed with `ImportError: Found an incompatible version of torchao. Found version 0.10.0, but only versions above 0.16.0 are supported`. The traceback reached `peft.import_utils.is_torchao_available()` from `pipe.load_lora_weights()`. The server uses FP16 plus CPU offload and does not request torchao quantization. Bootstrap now detects and removes optional torchao below 0.16.0 before starting the server, preserving CUDA PyTorch; the live outcome remains unverified.

The Supervisor's subsequent Kaggle run completed with exit code 0 and printed `HAIR CAPSTONE GPU SERVER READY`. It kept CUDA-enabled PyTorch 2.10.0+cu128 on a Tesla T4, removed optional torchao 0.10.0, reused the pinned Base snapshot, verified the TRAIN-001 adapter SHA-256, and started a GPU server with Base plus LoRA loaded once. Bootstrap started a Cloudflare Quick Tunnel and passed its public `/health` check. The temporary URL is session-specific and is intentionally not recorded in Git. No real `/generate` request or local browser round trip is evidenced yet.

The Supervisor subsequently reported a successful real request from the local browser through the Kaggle runtime and supplied original/result images. The source image was RGBA 1024×1536 with substantial transparency; the result was RGB 512×512 with a crew cut and changed gray background. Local API code converts the source to RGB, and the remote FLUX pipeline generates an opaque RGB image. The end-to-end route is functional by Supervisor report, while exact background and facial preservation are not verified. See [TRAIN-001](../docs/experiments/TRAIN-001.md).

The controlled expansion code now reads the three existing real styles, prompts, and frozen adapter hash from `backend/app/style_registry.json`; the frontend/API request shape is unchanged. The Kaggle server still loads TRAIN-001 through the proven single-adapter call at startup. For a future enabled adapter, the code validates its per-adapter hash and style claims, unloads the current LoRA, loads the requested LoRA, and restores TRAIN-001 after a switch failure when possible; one request is handled at a time. Local fake-pipeline tests exercise routing and restoration. A real T4 multi-adapter switch has not run and TRAIN-002 is not enabled. `scripts/paired_dataset.py` provides manifest-calculated, human-reviewed pair finalization for DATA-002+; DATA-001 code is unchanged. `scripts/data002_source_audit.py` decoded and sheeted 18 candidate source classes without extracting the full archive. See [DATA-002 proposal](../docs/data/DATA-002-proposal.md).

DATA-002 local selection was revised after a Philippine-priority audit of the source photographs. The current ten classes are `CurtainedHair`, `undercutSidepart`, `Fauxhawk`, `UndercutPompadour`, `PonyTail`, `PixieCut`, `ShoulderLenHair`, `WaveHair`, `shag`, and `Bun`. The manifest contains 120 explicit source members, 100 train and 20 validation identities, 2–4 screened reserves per class, unique selected SHA-256 values, and a balanced two-neighbor target graph. `docs/data/DATA-002-manifest.json` is frozen for review under SHA-256 `8ee456a102544cbc6f616bbc833fced0037a61f5c5e5301a052bc9f3d1562b9e`; the prior technical-shortlist manifest is archived and its hash superseded. `notebooks/data002_generate_kaggle.py` requires an explicit matching approval hash for GPU generation and writes pending-review metadata; no DATA-002 GPU generation has run. See [DATA-002 selection and handoff](../docs/data/DATA-002.md).
